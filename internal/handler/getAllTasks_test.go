package handler

import (
	"testing"
)

func TestHandler_GetAllTasks(t *testing.T) {
	//cnf := config.Conf{Cache: config.CacheConf{DefaultExpiration: 0, CleanupInterval: 0}, LogLevel: "info"}
	//log := logger.New(&cnf)
	//s := service.New(&cnf, log)
	//h := New(s, log)
	//f := fiber.New()
	//err := h.GetAllTasks(f)
}
